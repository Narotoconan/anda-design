# 消防器材进销存系统 UI 项目规范

## 1. 项目目的

本项目用于设计消防器材进销存系统的 UI 展示页面。

用户会在每次任务中说明需要设计的页面、需要呈现的信息与元素，以及其他具体要求。AI 应根据当次需求，运用专业的 UI/UX 设计知识完成现代、美观、清晰且具有良好视觉品质的页面设计。

本项目只进行 UI 页面设计与前端展示，不开发可投入实际使用的完整业务系统。

## 2. 最高执行原则

以下规则必须严格遵守：

1. 用户在当前任务中提出的要求具有最高优先级。
2. 只设计用户当前要求的页面，不擅自扩大任务范围。
3. 不在本规范中预设任何具体页面、模块、信息、字段、元素、文案、数据、业务流程、页面结构、配色或视觉风格。
4. 具体页面包含什么、展示什么以及如何组织，均以用户届时提供的需求为准。
5. 不得将之前页面中的临时设计决定自动视为整个项目的永久规则，除非用户明确要求延续或复用。
6. 不得因为系统属于某个行业，就擅自添加用户未要求的业务内容或功能。

## 3. 需求理解与处理

开始设计前，应准确理解用户当次提供的页面目标、展示内容、必要元素、视觉偏好和交付要求。

- 已经明确的内容必须完整落实，不遗漏、不弱化、不擅自替换。
- 不重复询问用户已经说明的信息。
- 如果缺少的信息会直接影响页面的核心结构或设计方向，应先进行简短、具体的确认。
- 对不影响整体方向的次要细节，可以基于专业设计判断合理补充，但不得借此改变需求或增加新的业务范围。
- 如果用户提供参考图、参考网站或已有页面，应理解并延续其设计意图，同时保证最终结果与当前项目协调。
- 当用户要求修改时，应针对反馈准确调整，不固守之前的方案。

## 4. UI 设计质量要求

每次页面设计都应达到以下标准：

- **现代美观**：整体观感精致、协调，避免陈旧、粗糙或明显套用默认模板的设计。
- **专业可信**：符合企业级管理系统的使用语境，视觉表达克制、稳定、有秩序。
- **层级清楚**：重点突出，主次明确，用户能够快速理解页面信息。
- **排版合理**：布局、对齐、间距、留白、尺寸和比例经过有意识的设计，不随意堆放内容。
- **视觉统一**：同一项目中的颜色、字体、图形、圆角、边框、阴影和交互表现保持一致。
- **易读易用**：文字清晰，信息密度合理，操作意图容易理解，不以装饰效果牺牲可读性。
- **贴合需求**：设计服务于用户当前提出的页面目标，不为展示设计技巧而加入无关内容。
- **细节完整**：页面不应呈现明显的错位、溢出、遮挡、样式冲突或未完成状态。

不得以“能显示”为完成标准。页面应经过专业的视觉判断和细节打磨，体现明确的设计方向，而不是简单拼装界面。

## 5. 功能边界

本项目默认只制作静态 UI 展示效果。

除非用户明确要求，否则不得主动实现：

- 后端服务、数据库或真实接口。
- 真实数据的读取、提交、同步或持久化。
- 登录认证、权限控制或生产级安全机制。
- 完整业务逻辑、真实业务流程或第三方系统集成。
- 与当前 UI 设计无关的工程能力、基础设施或功能扩展。

页面中出现的可操作界面默认只用于展示视觉效果，不代表需要实现对应业务能力。仅在用户明确提出时，才制作必要的纯前端演示交互；此类交互也不得扩展为真实功能开发。

## 6. 前端实施要求

- 修改前先检查项目现有结构、技术栈、依赖、组件和样式规范。
- 优先延续并复用现有方案，保持整个项目的视觉与代码一致性。
- 不随意更换技术栈，不引入与任务价值不匹配的复杂依赖。
- 代码应结构清楚、易于维护，避免无意义的重复和过度工程化。
- 只修改完成当前任务所必需的文件，不破坏或删除用户已有内容。
- 所有展示内容使用前端静态方式处理，不为静态页面搭建后端能力。
- 默认使用简体中文；如果用户指定其他语言，则以用户要求为准。

## 7. 公共后台壳层复用规范

项目已经将管理后台的侧边栏和顶部栏提取为公共组件。除非用户明确要求使用不同的页面壳层，后续新增后台页面必须复用该公共组件，不得在页面中重新复制或自行实现一套侧边栏、顶部栏。

### 7.1 公共文件

- `css/common.css`：统一管理后台壳层的布局、颜色、字体、材质、响应式行为和无障碍样式。
- `js/admin-shell.js`：统一生成侧边栏与顶部栏的 HTML 结构、导航高亮、顶部层级路径、同步状态和用户信息。
- 页面级 CSS 只负责当前页面的业务内容，不得重复定义 `.sidebar`、`.topbar`、`.side-*`、`.top-*` 等公共壳层样式。

### 7.2 新页面接入方式

每个新增后台页面必须：

1. 在 `<head>` 中引入公共 CSS 和公共组件脚本：

   ```html
   <link rel="stylesheet" href="../css/common.css">
   <script src="../js/admin-shell.js" defer></script>
   ```

   当前业务页面按一级目录组织：货物页面放在 `goods/`，基础资料页面放在 `foundation/`。以上路径以一级业务目录中的页面为例；新增页面应按实际目录深度调整相对路径。

2. 为 `<body>` 添加 `admin-shell` 类。
3. 在页面主要内容 `<main>` 之前放置公共组件：

   ```html
   <body class="admin-shell">
     <a class="skip-link" href="#main-content">跳到主要内容</a>
     <admin-sidebar active="goods-list"></admin-sidebar>
     <admin-topbar trail="库存管理|货物列表|当前页面"></admin-topbar>

     <main id="main-content" tabindex="-1">
       <!-- 当前页面业务内容 -->
     </main>
   </body>
   ```

### 7.3 组件配置约定

- `admin-sidebar` 的 `active` 属性控制当前导航高亮。
- 当前支持的导航标识包括：`overview`、`purchase`、`sales`、`goods-list`、`goods-detail`、`stocktake`、`transfer`、`flows`、`foundation`、`goods-categories`、`brands`、`suppliers`。
- 详情页使用 `active="goods-detail"`，侧边栏会关联高亮“货物列表”，但不会错误地把“货物列表”标记为当前页面。
- `admin-topbar` 的 `trail` 属性使用半角竖线 `|` 分隔层级，最后一项自动作为当前页面。
- `sync-text` 可覆盖顶部同步状态文案。
- `user-name`、`user-role`、`user-avatar` 可覆盖默认用户信息；未传入时使用公共默认值。
- 页面内部用于业务内容的二级面包屑可以按需求保留，但不得重复实现顶部栏中的公共层级导航。

### 7.4 修改与扩展原则

- 需要调整所有页面的侧边栏、顶部栏、品牌信息、用户信息或公共导航时，只修改 `js/admin-shell.js` 和 `css/common.css`。
- 新增公共导航入口时，应在 `js/admin-shell.js` 中统一增加导航项与对应 `active` 标识，不得只在单个页面中追加。
- 单个页面只能通过组件属性表达当前导航和顶部层级，不得复制组件渲染后的 `<aside>` 或 `<header>` 内容。
- 必须保持公共组件的语义结构和无障碍信息，包括主导航标签、当前位置、`aria-current`、跳转主内容链接以及键盘焦点样式。
- 公共组件必须保持纯前端、无框架、无后端依赖，确保 HTML 页面可直接预览。

### 7.5 验证要求

新增或修改页面后，应确认：

- 页面只生成一个侧边栏和一个顶部栏。
- 侧边栏高亮与当前页面匹配，顶部层级路径完整。
- 桌面端、收窄侧边栏状态和移动端布局均无内容遮挡或横向溢出。
- 公共组件脚本加载无错误，页面原有业务交互不受影响。

## 8. 字体大小与排版层级规范

所有页面、公共组件和业务模块必须共用同一套字体大小规范。以 `14px` 作为系统正文与常规界面文字的基础字号，不得为单个页面或模块另行创建一套字号体系。

### 8.1 统一字号令牌

字体大小必须由 `css/common.css` 中的全局令牌统一管理。全项目只允许使用以下字号层级：

| 令牌 | 字号 | 默认用途 |
| --- | ---: | --- |
| `--type-caption` | `12px` | 辅助说明、时间、备注、表头、次要元数据和紧凑状态文字 |
| `--type-base` | `14px` | 正文、导航、表格内容、表单标签、输入内容、按钮和常规界面文字 |
| `--type-emphasis` | `16px` | 需要适度强调的内容、卡片标题和小型标题 |
| `--type-section` | `20px` | 页面内主要分区标题、弹窗标题和重要模块标题 |
| `--type-title` | `28px` | 页面主标题和主要数据展示 |
| `--type-display` | `32px` | 少量需要最高视觉层级的展示性标题或核心数据 |

`--type-data` 可以作为数据展示的语义别名，但其字号必须与上述现有层级之一一致，不得形成新的字号。若公共文件中缺少所需令牌，应在 `css/common.css` 中统一补充，不得只在单个页面内声明。

### 8.2 字号使用规则

- `14px` 是默认字号。没有明确层级理由时，一律使用 `--type-base`。
- 同一种信息角色在不同页面、不同模块中必须使用相同字号，不得因容器、卡片或页面不同而随意变化。
- 优先通过字重、颜色、间距和布局建立层级；不得为了制造差异而新增 `13px`、`15px`、`17px`、`18px`、`22px`、`25px` 等零散字号。
- 可见界面文字不得小于 `12px`。不得使用 `9px`、`10px` 或 `11px` 字号压缩内容；空间不足时应调整布局、宽度、换行或信息密度。
- `28px` 和 `32px` 只用于页面最高层级或关键数据，不得在普通模块中大面积使用。
- 标题的 HTML 语义层级与视觉字号应分别处理；不得依赖浏览器对 `h1` 至 `h6` 的默认字号。
- 不得在 HTML 的 `style` 属性或页面级 CSS 中随意写入未经规范定义的 `font-size` 数值。
- 新增或修改页面时，应使用字体令牌替代裸字号；不得重新定义全局字体令牌的值。

### 8.3 行高与字重协调

- `12px`、`14px` 和 `16px` 文字默认使用 `1.5` 行高；多行辅助说明可使用不超过 `1.65` 的行高。
- `20px` 标题默认使用约 `1.35` 行高；`28px` 和 `32px` 标题或数据默认使用 `1.2` 至 `1.25` 行高。
- 常规正文默认使用 `400` 字重，强调内容优先使用 `500` 或 `600`，最高层级标题可使用 `600` 或 `700`。
- 同类文字的字号、行高和字重必须成套一致，不得只统一字号而保留互相冲突的排版参数。

### 8.4 Tailwind CSS 使用要求

- 使用 Tailwind CSS 时，必须将字号类映射到上述项目字号令牌，或直接使用引用全局令牌的写法。
- 不得直接混用 Tailwind 默认字号体系与项目字号体系；使用前必须确认实际字号与本规范完全一致。
- 不得通过任意值类创建规范之外的字号。允许使用类似 `text-[var(--type-base)]` 的写法引用公共令牌。
- 同一种文字角色应复用相同的类或组件样式，不得在每个页面重复拼装不同的字号组合。

### 8.5 响应式与例外

- 正文和常规界面文字在不同断点下应保持 `14px`，不得为了适配而整体缩小字号。
- 小屏幕上确需降低标题层级时，只能下降到相邻的既有字号令牌，不得生成新的中间字号。
- 为避免移动端浏览器聚焦表单时自动缩放，触屏小屏设备上的输入框、下拉框和文本域允许使用 `16px`；这是技术例外，不代表移动端基础字号改为 `16px`。
- 特殊展示内容只有在用户明确要求时才能突破该字号范围，并应保持为局部例外，不得改变全局规范。

### 8.6 字号验收要求

新增或修改页面后必须确认：

- 页面使用的所有字号均来自统一令牌，且没有未经允许的裸字号或临时字号。
- 相同角色的文字在侧边栏、顶部栏、页面内容、弹窗和不同业务模块中保持一致。
- 正文与常规控件以 `14px` 为基础，辅助文字不小于 `12px`。
- 标题层级数量合理，同一页面没有过多相近但不同的字号。
- 响应式状态下没有通过缩小文字掩盖布局问题。

## 9. 输出格式

- 需要输出页面文件时，默认以 `.html` 文件交付，并确保可以在浏览器中直接预览。
- 页面样式可以使用 Tailwind CSS，也可以根据设计需要补充自定义 CSS。
- 除非用户明确要求，不创建 React、Vue 等框架项目，不输出其他页面实现格式。
- 不得因为使用 Tailwind CSS 而改变、删减或简化用户提出的设计要求。

## 10. 验证与交付

完成页面后必须自行检查：

- 是否完整满足用户当次提出的全部要求。
- 是否添加了用户未要求的页面、内容、元素或功能。
- 是否具有清晰、现代、专业且统一的视觉表现。
- 是否存在布局、排版、尺寸、对齐、遮挡或溢出问题。
- 是否在目标展示尺寸下保持良好的可读性和完整性。
- 是否误做了本项目范围之外的真实业务能力。

在具备运行和预览条件时，应实际查看页面效果并修正明显问题后再交付。最终说明应如实、简洁，只描述已经完成的 UI 设计内容，不宣称未实现的功能。
